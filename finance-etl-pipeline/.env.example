# Copia este archivo como ".env" (sin el ".example") y pon tus valores reales.
# El archivo ".env" real NUNCA se sube a GitHub (está en .gitignore).

DB_HOST=localhost
DB_PORT=5432
DB_NAME=finance_pipeline
DB_USER=postgres
DB_PASSWORD=tu_contraseña_de_postgres_aqui